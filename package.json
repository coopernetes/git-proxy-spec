{
  "name": "git-proxy-spec",
  "version": "0.0.0",
  "private": true,
  "description": "A working-draft specification for a policy-enforcing Git proxy.",
  "license": "Apache-2.0",
  "author": "Thomas Cooper",
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.md": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.4.2"
  }
}
